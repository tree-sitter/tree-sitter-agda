==================
Modules
==================

module _ where

module Test where
module Test.Module where

---

(source_file
    (module (anonymous_name))
    (module (qualified_name))
    (module (qualified_name)))
