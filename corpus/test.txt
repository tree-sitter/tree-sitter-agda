==================
Test
==================

a : Set
a = A

b : Set
b = B

---

(source_file
    (function_clause
        (lhs
            (atomic_expr (qualified_name)))
        (rhs
            (expr
                (atomic_expr)
                (atomic_expr (qualified_name))
                (expr (atomic_expr (qualified_name)))))))
